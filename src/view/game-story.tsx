import * as React from "react";
import styled from "styled-components";

const Container = styled.div`
  flex: 1;
`;

export function GameStory(): React.FC {
  return <Container>Game story text goes here</Container>;
}
